{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "node index.js",
    "test": "nyc --reporter=lcov --reporter=text mocha --exit --timeout 10000 --recursive unit_tests/*.js",
    "coverage": "nyc report --reporter=text-lcov | coveralls"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "c8": "^7.3.5",
    "chai": "^4.2.0",
    "coveralls": "^3.1.1",
    "mocha": "^8.1.3",
    "mocha-lcov-reporter": "^1.3.0"
  },
  "dependencies": {
    "-": "^0.0.1",
    "nyc": "^15.1.0",
    "save": "^2.9.0"
  }
}
